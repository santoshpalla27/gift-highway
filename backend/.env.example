APP_ENV=development
PORT=8080
DATABASE_URL=postgres://app:secret@localhost:5432/appdb?sslmode=disable
JWT_SECRET=change-me-in-production-use-32-chars-minimum
JWT_EXPIRY=15m
REFRESH_EXPIRY=720h
ALLOWED_ORIGINS=*
RATE_LIMIT_RPS=100
RATE_LIMIT_BURST=200

# ── Cloudflare R2 (file storage) ──────────────────────────────────────────────
R2_ACCOUNT_ID=
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=app-data
R2_PUBLIC_URL=https://<account_id>.r2.cloudflarestorage.com/<bucket>

# ── Audit / backup (optional — leave blank to disable) ────────────────────────
# Uses the same R2 credentials above. Set AUDIT_R2_BUCKET to your bucket name.
AUDIT_R2_BUCKET=
SMTP_USER=gifthighway.audit@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx
AUDIT_EMAIL_TO=youremail@gmail.com

# ── Push notifications (optional) ─────────────────────────────────────────────
# Get token at: https://expo.dev/accounts/[your-account]/settings/access-tokens
EXPO_ACCESS_TOKEN=
